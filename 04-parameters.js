//*functions are reusable

function hi(name){
    console.log('Hi ${name}!')
}
hi('Danielle');
hi('Zach');

//1-The parameter(s) we need to take into the function
//2-using string interpolation, we cna refer to the parameter we passed
//3-This is where we define what the parameter's value will be

function pet (animal) {
    console.log('My puppy' name is ${animal}!')
}

    pet('Mo');
    pet('George');
    pet('Nugget');

function person (fname, lName) {
    let wName = fName + lName;
    console.log ('my name is ${'Shabana'})
}
    
    person('shabana', 'deckinga')

